# the "user" side - a netshoot pod we curl the Service from (has curl, dig, tcpdump, ip).
apiVersion: v1
kind: Pod
metadata:
  name: netshoot
  labels: { app: netshoot }
spec:
  containers:
    - name: netshoot
      image: nicolaka/netshoot:latest
      command: ["sleep", "36000"]
